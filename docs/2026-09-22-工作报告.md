# MAO 项目工作日报告 — 2026-09-22

> 报告人：小弟 ｜ 目标：重新查看项目、排查问题 ｜ 模式：Agent（可直接改文件）

## 一、本日目标

大哥指令：**"重新查看项目，排查项目中的问题"**。

按项目行为基准（先查证再开口、结论先行、证据挂最前）执行：重读全部真实源码、跑真实测试，输出分级问题清单，待确认后实施修复。

## 二、当前架构现状（实测，非记忆）

MAO 已演进为 **单一 Bridge 内核 + 三外壳** 架构：

- 入口：`python -m agent bridge`（裸 JSON 行协议）/ `mcp`（标准 MCP server）/ `call <cmd> [json]`（单发）。
- 原 `chat`/`run`/`pipeline`/`web` 入口已移除；`agent/` 包无 `import agent.web`、`FastAPI`、`uvicorn`。
- 模块规模：14 个 `.py`（原 17），`orchestrator` 拆出 `voting.py`、`builtin` 拆出 `shell_safety.py`、新增 `constants.py`。
- 外部主 AI 驱动模式：主智能体由外部 AI 担任，MAO 做无状态执行器 + 工人池（纯文本子 AI）。

> 结论：早期审计清单中"Web lifespan / Session 缓存 / Pipeline 素材长度"等条目在当前单内核架构下**已不适用**，本轮未误报。

## 三、质量实证基线

| 维度 | 命令 | 结果 |
|---|---|---|
| 静态检查 | `ruff check .` | **All checks passed** |
| 单元测试 | `pytest -q` | **137 passed** |
| 变异测试 | `tests/mutation_check.py` | **9 OK / 5 SKIP** |

> 变异测试 5 个 SKIP 不是"代码有 bug"，而是锚点文件失效（见第四节）。

## 四、排查中发现的关键事实：并行会话重构

排查中途发现**另一会话正对本仓库做重构，且未提交**（git 脏）。已落地的真实改动：

- `config.py`：`raw` 兜底 + `cacheable` 修复（path 显式时不污染默认缓存）。
- `orchestrator.py`：未知工人名不再静默丢弃，带回 `ST_MISSING`（审计 P1 已修复）。
- `voting.py`（新）：`threshold` 过 `Config.as_float()`（bridge 传字符串 `"0.5"` 不再 TypeError）。
- `shell_safety.py`（新）：命令安全扫描从 `builtin.py` 抽出。
- `constants.py`（新）：状态码集中定义。

重构后已重测：`ruff` 仍全过、`pytest` 收集正常（137 passed）。

## 五、问题清单（按风险分级）

均**不影响现有双路径可用性，无功能性 bug**；属质量/一致性/回归保护缺口。

| 级别 | 位置 | 问题 | 建议 |
|---|---|---|---|
| **中** | `tests/mutation_check.py` M8/M10/M12/M16/M19 | **5 个变异锚点失效**：重构把代码移到 `voting.py`/`shell_safety.py`，锚点仍指旧文件→SKIP。失活含 **2 安全检查**（M16 引号屏蔽防注入、M19 解释器内联代码扫描）+ **3 投票正确性断言**（M8 计票过滤非成功票、M10 缺省名单限额、M12 分母剔无效票），回归保护当前为空 | 纯改文件路径（M8/M10/M12→`core/voting.py`，M16/M19→`tools/shell_safety.py`）；零运行时风险，项目硬要求，**建议优先修** |
| **中** | `skills_manager.py:146` vs `bridge.py`/`mcp_server.py` | **技能工具命名分裂**：registry 名 `execute_skill_script` 与顶层/MCP 名 `run_skill_script` 并存，均调 `SkillManager.run_script`；`call_tool` 走前者、顶层协议与 MCP 走后者，外部主易调错名 | 统一为 `run_skill_script` |
| **中** | `config.py` `_load_dotenv`/`_dotenv_keys` | **`.env` 删除 key 不生效**：`_dotenv_keys` 只增不清，reload 时 `os.environ` 残留旧值，直至重启 | reload 时回收"本次文件未出现"的 key |
| **中** | `orchestrator.py` `vote()` | **`timeout` 两阶段各计一次**：整体最长 ≈2× 传入值（`select_best` 只 1×），与 REQUIREMENTS §4「收集阶段整体限时」描述不符 | 折半或文档明确语义 |
| 低 | `tools/base.py` `ToolRegistry.restrict()` | **死代码**：Web 移除后无调用方 | 删除或标废弃 |

## 六、下一步建议

修复顺序：**#1（恢复变异保护，机械零风险）→ #2 → #3 → #4 → #5**；每项落地后重跑 `ruff` / `pytest` / `mutation` 复核。

本轮**未改动任何产品代码**，仅产出审计文档与今日记录，待大哥确认"全部修改"或指定条目后实施并真实验证。

## 七、本日提交说明

按大哥指令"提交项目推送到 GitHub"，将当前工作树全部改动（含并行会话未提交的重构 + 本日审计/报告文档）一并提交并推送：

- 实测基线稳定（ruff 全过、pytest 137 passed），纳入提交无回归风险。
- `.gitignore` 已屏蔽 `.venv` / `.workbuddy` / `data` / `.env`，不会带密钥或虚拟环境。
- 详细分级清单见 `docs/2026-09-22-重新审计清单.md`。
