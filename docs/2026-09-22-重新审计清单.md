# 2026-09-22 重新审计清单（MAO 单内核架构，v2 更新）

> 排查方式：全量重读 `agent/` 当前源码 + 配置/测试，对照 `REQUIREMENTS.md` 行为基准，
> 以真实命令（ruff / pytest / mutation）核对现状，**不靠记忆**。
> 注意：排查中途并行会话正在重构本仓库（未提交），本文已基于重构后真实代码核对。

## 一、现状基线（实测，2026-09-22 17:20）

| 项 | 结果 |
|---|---|
| git 状态 | **不干净**：并行会话重构中，6 文件 modified + 4 新文件未追踪（见下方清单），HEAD 仍为 `9af18ff` |
| `ruff check .` | All checks passed |
| `pytest -q` | **137 passed（全过）** |
| 变异测试 | **9 OK / 5 SKIP**（5 个锚点因重构失效，见第三节「新发现 #1」） |
| 结构 | `agent/` + `core/`(orchestrator/health/llm/voting/constants) + `tools/`(base/builtin/mcp_client/shell_safety) + `bridge`/`config`/`skills_manager`/`mcp_server` |
| 架构 | 单一 `Bridge` 内核 + 三外壳（bridge / mcp / call）；`chat/run/pipeline/web` 入口已移除 |

并行会话重构（未提交）改动文件：
- modified：`agent/config.py`、`agent/core/orchestrator.py`、`agent/skills_manager.py`、`agent/tools/builtin.py`、`agent/tools/mcp_client.py`、`tests/test_core.py`、`tests/test_resilience.py`
- 新增：`agent/core/constants.py`、`agent/core/voting.py`、`agent/tools/shell_safety.py`、`tests/test_audit3_fixes.py`

## 二、架构演进说明（避免把历史项当新问题）

`docs/2026-09-21~22-工作记录.md` 记录了 4 轮审计修复，其中大量涉及 `web/`、`Session`、
`pipeline.py`、`session.messages_with`、`events.py`。**这些模块在当前单内核架构下已不存在**
（Web 入口移除、pipeline 移除、Session 无状态化）。因此以下历史审计项**当前已不适用、非本次问题**：

- Web lifespan `_stop_event` 复位 / 漏清 `_session_orch`（Web 已移除）
- Session 缓存命中不受冷却影响 / `_maybe_flush` 并发写盘顺序（Session 已移除）
- Pipeline 阶段间素材无长度约束（pipeline 已移除）
- `/new` 主智能体重置（Agent 选主入口已移除）

并行会话本轮重构（已确认落地）顺手修复了早期标记的 3 项：
- `config.py` P2：`raw` 兜底 + `cacheable = path is None`（显式 path 不污染默认缓存）
- `orchestrator.py` P1：未知工人名不再静默丢弃，由 `ask_result` 带回 `status=missing`
- `voting.py` P1：`threshold` 过 `as_float`，防 bridge 传 `"threshold":"0.5"` 字符串抛 `TypeError`

## 三、本次新发现问题（按风险分级）

| 级别 | 位置 | 问题 | 影响 / 建议 | 状态 |
|---|---|---|---|---|
| **中** | `tests/mutation_check.py` M8/M10/M12/M16/M19 | **5 个变异锚点失效**：重构把代码从 `orchestrator.py`/`builtin.py` 抽到 `voting.py`/`shell_safety.py`，但 5 个锚点仍指向旧文件 → 变异测试打印 SKIP（不误改源码，但**回归保护为空**）。失活的 5 处含 **2 条安全检查**（M16 引号屏蔽防注入、M19 解释器内联代码扫描）+ **3 条投票正确性断言**（M8 计票过滤非成功票、M10 缺省名单限额、M12 分母剔除无效票）。 | 纯测试文件路径修正：M8/M10/M12 改指向 `agent/core/voting.py`、M16/M19 改指向 `agent/tools/shell_safety.py`。零运行时风险，恢复项目硬要求的变异保护。**建议优先修**。 | 待修 |
| **中** | `skills_manager.py:146` vs `bridge.py:141`/`mcp_server.py:70` | **技能脚本工具命名分裂**：`register_skill_tools` 注册名 `execute_skill_script`，而 bridge 顶层指令与 MCP 工具名是 `run_skill_script`；两者都调 `SkillManager.run_script`，功能完全相同但名字两套。同一 Bridge 实例里 `call_tool` 走 `execute_skill_script`、顶层协议与 MCP 走 `run_skill_script`，外部主按 `list_tools` 看到的是前者、按协议文档该传后者，易调错名。 | 统一为 `run_skill_script`（`register_skill_tools` 改注册名），消除双路径冗余。 | 待修 |
| **中** | `config.py` `_load_dotenv` / `_dotenv_keys` | **`.env` 删除 key 不生效**：模块级 `_dotenv_keys` 只增不清，reload 时只清 `_missing_logged`/`_bad_value_logged`，不清 `_dotenv_keys`；从 `.env` 删掉某行 key 后，`os.environ` 仍残留旧值，直至进程重启才生效。 | reload 时回收"`_dotenv_keys` 中本次文件未出现"的 key（只清 dotenv 注入的，不动 shell 预置）。 | 待修 |
| **中** | `orchestrator.py` `vote()`（collect + `_run_ballot`） | **`timeout` 在两阶段各计一次**：传入的 `timeout` 在「收集候选」(`collect`) 与「投票」(`_run_ballot`) 各作为该阶段整体限时用一次，整体最长 ≈ 2×；而 `select_best` 只计 1×。REQUIREMENTS §4 把 `timeout` 描述为「收集阶段整体限时」，未说明 vote 两阶段，调用方易误判 vote 总耗时。 | 要么把传入 `timeout` 在两阶段折半，要么在 docstring/REQUIREMENTS 明确「vote 的 timeout 为每阶段限时」。 | 待修 |
| 低 | `tools/base.py` `ToolRegistry.restrict()` | **死代码**：Web 移除后无任何调用方（REQUIREMENTS §6 已声明「不再有会话级工具白名单」）。 | 删除或加 `# 已废弃` 注释。 | 待修 |
| 观察 | `health.py` `quarantined()` | 只读查询会修改内存 `active` 列表（登记运行日），但 `_write_payload` 在锁外、仅 `active` 不持久化（注释已说明 active 仅运行痕迹、不参与判定）。 | 无功能影响，观察项不改 |
| 观察 | `orchestrator.py` `_wait_gather` | 整体限时到点后，已 `submit` 但仍在跑的工人线程只能等 LLM 超时（`llm.timeout`）自然收尾；`_effective_timeout` 已留 30s 余量。 | 工作记录已标注，无资源泄漏，观察项 |

## 四、结论

代码整体质量高：结构化成败契约（ok/status）已贯穿内核三外壳，ruff/pytest 全过。
**无功能性 bug**，仅 4 个中风险（1 测试质量 + 3 产品）+ 1 个低风险（死代码）待清理，均不影响现有
双路径可用性。其中「变异锚点失效」属测试质量，却正好让 2 条安全检查 + 3 条投票正确性断言的回归保护
沦为空白，**建议优先修复**（机械、零风险）。

## 五、建议下一步

确认后统一修复，并按安全/风险顺序：
1. **先修 `mutation_check.py` 5 锚点**（改文件路径）→ 跑 `ruff`/`pytest`/`mutation` 复核至 14/14 OK；
2. 统一 `execute_skill_script` → `run_skill_script`；
3. 修 `.env` 删除 key 不生效（`_dotenv_keys` 回收）；
4. `vote()` 的 `timeout` 折半或文档明确语义；
5. 删除 `ToolRegistry.restrict()` 死代码。
