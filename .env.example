# ============================================
# .env 示例 —— 中转站免费 token 场景
# 复制本文件为 .env 后填写你的密钥。
# 用法（PowerShell）:  Copy-Item .env.example .env
#
# 中转站特点：一个 API Endpoint + 一个 KEY，下面可以挂多个 Model。
# 每个模型可单独标注上下文窗口（token），用 @ 附加在模型名后：
#   gpt-4o-mini@128k  → 128k=128×1024 token；也可写纯数字如 200000
#   不标 @ 的模型按默认 128k 处理。历史对话会按此窗口自动裁剪，防溢出丢回复。
# 临时屏蔽某模型 = 在该模型前加 #。
# 注意：_ENDPOINT 结尾一般是 /v1；_MODELS 用英文逗号分隔多个模型。
# .env 以点开头属保密文件，AI 约定不读取、.gitignore 绝不提交。
# ============================================

# ---------- 中转站 A ----------
NODE_A_ENDPOINT=https://endpoint-a.example.com/v1
NODE_A_KEY=sk-xxxxxxxxxxxxxxxx
NODE_A_MODELS=model-a-1@128k,model-a-2@64k,#model-a-3@32k

# ---------- 中转站 B ----------
NODE_B_ENDPOINT=https://endpoint-b.example.com/v1
NODE_B_KEY=sk-yyyyyyyyyyyyyyyy
NODE_B_MODELS=model-b-1@200000,model-b-2

# ---------- 兜底单模型（不走中转站的直连入口）----------
# 这三个变量对应 config.yaml 的 `llm:` 段（池里节点全挂时走它 / solo 模式用它）。
# 变量名必须与 config.yaml 里的 ${...} 完全一致，否则填了也不生效。
#   LLM_ENDPOINT / LLM_MODEL 可不填：config.yaml 里带了默认值
#   LLM_API_KEY  必填（不填则兜底单模型不可用，只剩中转站池）
LLM_ENDPOINT=https://api.deepseek.com/v1
LLM_API_KEY=sk-zzzzzzzzzzzzzz
LLM_MODEL=deepseek-chat
